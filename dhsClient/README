run 

  dhsServer <servername>

on the server side, and then run 

  dhsClient <clientname> <serverhost> <servername>

on the client.

For example:

on the server:

  dhsServer dhsServer &

on the client:

  dhsClient dhsClient bean dhsServer

